# Intern-At-Cognifyz-Technology
This project demonstrates the fundamentals of server-side rendering, form handling, and Node.js + Express server setup. It includes a basic workflow where users submit data through an HTML form, and the backend processes and renders the output dynamically using EJS templates
